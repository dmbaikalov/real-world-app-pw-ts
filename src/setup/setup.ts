// 	function getEnvVarString(name: string): string {
// const var = process.env.name
// if (isNotEmpty(var)) {
// return var
// }
// throw new Error(`Variable ${name} is empty not defined. Current value: ${var
// } `)
// }

// isNotEmpty(var: unknown): var is String {}